version: 1.0.{build}
configuration: Release_fdot
platform: x64
install:
 - set PATH=C:\Python27-x64;%PATH%
 - pip install numpy
 
build:
  project: win\VS2015\toast.sln
  verbosity: quiet

after_build:
 - make_python.bat
