function p = toastMeshNodeCount
%toastMeshNodeCount   - Return the node count of a TOAST mesh.
%
% Synopsis: n = toastMeshNodeCount(hMesh)
%    hMesh: mesh handle
%    n:     node count (integer)
%
% Returns the node count of a mesh, specified by a mesh handle.
