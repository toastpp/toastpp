function p = toastMeshElementCount
%toastMeshElementCount - Return the element count of a TOAST mesh.
%
% Synopsis: n = toastMeshElementCount(hMesh)
%    hMesh: mesh handle
%    n:     element count (integer)
%
% Returns the number of elements in a mesh, specified by a mesh handle.
