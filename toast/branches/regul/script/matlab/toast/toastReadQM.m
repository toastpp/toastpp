function p = toastReadQM
%toastReadQM          - Read a QM (source-detector) specification
%
% Synopsis: toastReadQM (hMesh, 'qmfile')
%   hMesh: mesh handle (e.g. from toastReadMesh)
%   qmfile: QM file name
%
% Reads a QM (source-detector) specification from a file into a TOAST mesh.
