#include <stdlib.h>
#include <math.h>
#include "mathdef.h"

// ==========================================================================
// function instantiations

#ifdef NEED_EXPLICIT_INSTANTIATION

//template double toast::min (const double x, const double y);
//template float toast::min (const float x, const float y);
//template int toast::min (const int x, const int y);

//template double toast::max (const double x, const double y);
//template float toast::max (const float x, const float y);
//template int toast::max (const int x, const int y);

#endif // NEED_EXPLICIT_INSTANTIATION
